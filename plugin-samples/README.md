Sample external plugin sources for Banana Republic.

Artifacts to build:
- `plugin-samples/dist/greedy-bot-plugin.jar`
- `plugin-samples/dist/sunburst-island-plugin.jar`

Both jars are compiled against the current project's `target/classes`.
